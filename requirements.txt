# ── 训练环境 ──
torch>=2.1.0
transformers>=4.45.0
datasets>=2.20.0
accelerate>=0.30.0
peft>=0.10.0          # 若使用 LoRA 微调
sentencepiece>=0.2.0
protobuf>=3.20.0

# ── 导出 ──
optimum[exporters]>=1.20.0   # ONNX 导出 (MNN 方案)
onnxruntime>=1.18.0

# ── 可选 (日志) ──
tensorboard>=2.15.0

httpx[socks]
